# Fast mainnet RPC (archive-capable). Used by fetch_chainlink.py via boa.
# A local node is strongly preferred: the AnswerUpdated scan issues many
# eth_getLogs / eth_getBlockByNumber calls.
ETH_RPC_URL=http://127.0.0.1:8545
